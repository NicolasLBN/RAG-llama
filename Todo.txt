Phase 1 : Préparation des Modèles & Infrastructure
[x] Télécharger le modèle de Chat/Texte :

Télécharger Qwen2.5-3B-Instruct-Q4_K_M.gguf (ou Llama-3.2-3B-Instruct-Q4_K_M.gguf) depuis Hugging Face.

Placer ce fichier .gguf dans le même dossier ./models que snowflake-arctic-embed-m-v2.0.q4_k_mlp_q8_attn.gguf.

[x] Créer le docker-compose.yml :

Configurer le service llama-embed sur le port 8081 (avec l'argument --embedding).

Configurer le service llama-chat sur le port 8080 (avec le modèle Qwen 3B).

[ ] Démarrer et valider l'infra :

Lancer docker compose up -d.

Vérifier avec curl que le port 8081 répond pour les embeddings et que 8080 répond pour le chat textuel.

Phase 2 : Ingestion des Manuels Utilisateurs
[x] Écrire le script d'ingestion (Python) :

Charger les fichiers PDF de 20 pages.

Découper le texte en blocs (chunks) de 300 à 500 caractères.

[x] Générer et stocker la mémoire documentaire :

Envoyer chaque bloc au port 8081 (http://localhost:8081/v1/embeddings) pour récupérer son vecteur.

Enregistrer dans la base vectorielle locale (ChromaDB ou FAISS) le doublon : [Vecteur, Texte brut de la page].

Phase 3 : Logique d'Interaction avec l'Opérateur (HMI)
[x] Étape A (Recherche) : Quand l'utilisateur pose une question sur l'HMI :

Envoyer la question au port 8081 pour obtenir son vecteur.

Interroger la base vectorielle locale pour sortir le paragraphe du manuel le plus proche.

[x] Étape B (Rédaction) :

Assembler le prompt final en français :

"Tu es l'assistant technique de la machine. Réponds à l'opérateur uniquement avec l'extrait suivant du manuel : [Paragraphe trouvé]. Question : [Question opérateur]"

Envoyer ce prompt au port 8080 (http://localhost:8080/v1/chat/completions).

Récupérer le texte rédigé et l'afficher sur l'écran HMI.

Phase 4 : Validation sur la machine 8 Go
[ ] Contrôle RAM : S'assurer avec htop ou docker stats que l'ensemble des 2 conteneurs + script n'excède pas ~2.5 Go à 3 Go de RAM.

[ ] Contrôle Vitesse : Ajuster le paramètre -t (nombre de threads CPU) dans le docker-compose pour que la réponse s'affiche en moins de 2 à 4 secondes.
